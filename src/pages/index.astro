---
import { getCollection } from 'astro:content';

// Layout import — provides basic page elements: <head>, <nav>, <footer> etc.
import BaseLayout from '../layouts/BaseLayout.astro';

// Component Imports
import CallToAction from '../components/CallToAction.astro';
import Grid from '../components/Grid.astro';
import Hero from '../components/Hero.astro';
import Icon from '../components/Icon.astro';
import Pill from '../components/Pill.astro';
import PortfolioPreview from '../components/PortfolioPreview.astro';

// Page section components
import ContactCTA from '../components/ContactCTA.astro';
import Skills from '../components/Skills.astro';

// Content Fetching: List four most recent work projects
const projects = (await getCollection('work'))
	.sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf())
	.slice(0, 4);

// Full Astro Component Syntax:
// https://docs.astro.build/basics/astro-components/
---

<BaseLayout>
	<div class="stack gap-20 lg:gap-24">
		<div class="wrapper stack gap-8 lg:gap-20">
			<header class="hero">
				<Hero
					title="Kengo Hoi"
					tagline="Welcome to my portfolio!"
					align="center"
				>
				</Hero>
			</header>
		</div>

		<!-- ========= ABOUT ========= -->
		<section id="about">
			<div class="wrapper stack gap-12 lg:gap-10">

				<!-- セクション見出し -->
				<header class="hero">
				<Hero
					title="about"
					tagline="私の簡単な自己紹介です。"
					align="center"
				/>
				</header>

				<!-- ========== 本体：左写真 / 右プロフィール ========== -->
				<div class="profile-grid">
				<!-- 左：写真 -->
				<div class="profile-photo">
					<img
					src="/assets/images/profile.jpg"
					alt="Hoi Kengo portrait"
					width="320"
					height="320"
					loading="lazy"
					/>
				</div>

				<!-- 右：プロフィールテキスト -->
				<div class="profile-text">
					<p><strong>名前：</strong>帆井 健悟 (Hoi&nbsp;Kengo)</p>

					<p>
					<strong>学歴：</strong><br />
					・2020/03 渋谷教育学園幕張高等学校 卒業<br />
					・2025/03 北海道大学 工学部 情報エレクトロニクス学科 情報理工学コース 卒業<br />
					・2025/04 北海道大学大学院 情報科学院 情報理工学コース 調和系工学研究室 在籍 (修士1年)
					</p>

					<p>
					<strong>スキル：</strong><br />
					C / HTML・CSS / Java / MATLAB / Python / Ruby / Linux /
					AIライブラリ (TensorFlow, PyTorch) / Arduino ほか
					</p>

					<p>
					<strong>興味のある技術分野：</strong><br />
					3DCG・XR・Interactive&nbsp;Graphics
					</p>
				</div>
				</div>
			</div>
		</section>

		<!-- ========= Skills ========= -->
		<section id="Skills">
			<div class="wrapper stack gap-12 lg:gap-10">

				<!-- セクション見出し -->
				<header class="hero">
				<Hero
					title="Skills"
					tagline="自分が持つスキルについての紹介です。"
					align="center"
				/>
				</header>


				<!-- 右：プロフィールテキスト -->
				<div class="profile-text">
					<p>
					<strong>スキル：</strong><br />
					C / HTML・CSS / Java / MATLAB / Python / Ruby / Linux /
					AIライブラリ (TensorFlow, PyTorch) / Arduino ほか
					</p>

					<p>
					<strong>興味のある技術分野：</strong><br />
					3DCG・XR・Interactive&nbsp;Graphics
					</p>
				</div>
			</div>
		</section>

		<!-- ========= Works ========= -->
		<section id="works"  class="py-10 lg:py-24  mb-20 lg:mb-24">
			<div class="wrapper stack gap-12 lg:gap-10">

				<!-- セクション見出し -->
				<header class="hero">
				<Hero
					title="works"
					tagline="自分のこれまでの制作・活動実績を紹介します"
					align="center"
				/>
				</header>

				<!-- ========= WORK LIST ========= -->
				<div class="works-grid">

				<!-- 1. ステレオカメラ E2E 自動運転研究 -->
				<article class="work-card">
					<h3>ステレオカメラを用いた<br class="sm:hidden" />E2E自動運転モデル</h3>
					<p class="meta">
					<span>北海道大学 / Astemo (共同研究)</span>
					<span>2023-10 – Present</span>
					</p>

					<p>
					従来の多センサ構成をステレオカメラ <em>単一</em> に集約し、
					TransFuser を改良して画像＋点群から加速度を予測する軽量
					E2E モデルを開発。<br />
					13 万フレームで学習した追従走行タスクで実用水準の性能を確認し、
					ステレオ専用アプローチの可能性を示唆。
					</p>

					<ul>
					<li>課題定義・検証設計・モデル設計をワンストップで推進</li>
					<li>大規模データ前処理を自動化するパイプラインを構築</li>
					<li>社内発表で<strong>高評価</strong>を獲得、継続開発中</li>
					</ul>
				</article>

				<!-- 2. 広告薬機法チェック LLM 自動化 -->
				<article class="work-card">
					<h3>薬機法チェック<br class="sm:hidden" />自動化システム (LLM)</h3>
					<p class="meta">
					<span>受託開発 / 化粧品広告</span>
					<span>2024-04 – 2024-08</span>
					</p>

					<p>
					化粧品広告が薬機法に抵触しないかを
					<strong>大規模言語モデル</strong>で自動判定する Web サービスを
					2 名体制で開発。私は判定ロジックと API 連携を担当。
					</p>

					<ul>
					<li>プロトタイプ → フィードバック → 改善を隔週で反復</li>
					<li>弁護士事務所・仲介企業など多様な利害関係者と調整</li>
					<li>納期内リリース達成、運用コストを大幅削減</li>
					</ul>
				</article>

				<!-- 3. アボカド価格予測 ML コンペ -->
				<article class="work-card">
					<h3>アボカド価格予測<br class="sm:hidden" />機械学習コンペ</h3>
					<p class="meta">
					<span>大学授業プロジェクト</span>
					<span>2023-01 – 2023-02</span>
					</p>

					<p>
					3 名チームで米国アボカド価格を予測。
					私は<strong>ベースコード</strong>を整備し、特徴量設計・
					ハイパーパラメータ探索を効率化。未経験チームながら
					20 チーム中<strong>6位</strong>を獲得。
					</p>

					<ul>
					<li>LightGBM / XGBoost / CatBoost を並列で評価</li>
					<li>GitHub + Issues でタスク管理、全員の生産性を底上げ</li>
					<li>リーダーシップとドキュメンテーションの重要性を学習</li>
					</ul>
				</article>

				</div>
			</div>
		</section>
	</div>
</BaseLayout>

<style>
	.hero {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 2rem;
	}

	.roles {
		display: none;
	}

	.hero img {
		aspect-ratio: 5 / 4;
		object-fit: cover;
		object-position: top;
		border-radius: 1.5rem;
		box-shadow: var(--shadow-md);
	}

	@media (min-width: 50em) {
		.hero {
			display: grid;
			grid-template-columns: 1fr;
			padding-inline: 2.5rem;
			gap: 3.75rem;
		}

		.roles {
			margin-top: 0.5rem;
			display: flex;
			gap: 0.5rem;
		}

		.hero img {
			aspect-ratio: 3 / 4;
			border-radius: 4.5rem;
			object-fit: cover;
		}
	}

	/* ====================================================== */

	.section {
		display: grid;
		gap: 2rem;
	}

	.with-background {
		position: relative;
	}

	.with-background::before {
		--hero-bg: var(--bg-image-subtle-2);

		content: '';
		position: absolute;
		pointer-events: none;
		left: 50%;
		width: 100vw;
		aspect-ratio: calc(2.25 / var(--bg-scale));
		top: 0;
		transform: translateY(-75%) translateX(-50%);
		background:
			url('/assets/backgrounds/noise.png') top center/220px repeat,
			var(--hero-bg) center center / var(--bg-gradient-size) no-repeat,
			var(--gray-999);
		background-blend-mode: overlay, normal, normal, normal;
		mix-blend-mode: var(--bg-blend-mode);
		z-index: -1;
	}

	.with-background.bg-variant::before {
		--hero-bg: var(--bg-image-subtle-1);
	}

	.section-header {
		justify-self: center;
		text-align: center;
		max-width: 50ch;
		font-size: var(--text-md);
		color: var(--gray-300);
	}

	.section-header h3 {
		font-size: var(--text-2xl);
	}

	@media (min-width: 50em) {
		.section {
			grid-template-columns: repeat(4, 1fr);
			grid-template-areas: 'header header header header' 'gallery gallery gallery gallery';
			gap: 5rem;
		}

		.section.with-cta {
			grid-template-areas: 'header header header cta' 'gallery gallery gallery gallery';
		}

		.section-header {
			grid-area: header;
			font-size: var(--text-lg);
		}

		.section-header h3 {
			font-size: var(--text-4xl);
		}

		.with-cta .section-header {
			justify-self: flex-start;
			text-align: left;
		}

		.gallery {
			grid-area: gallery;
		}

		.cta {
			grid-area: cta;
		}
	}

	/* ====================================================== */

	.mention-card {
		display: flex;
		height: 7rem;
		justify-content: center;
		align-items: center;
		text-align: center;
		border: 1px solid var(--gray-800);
		border-radius: 1.5rem;
		color: var(--gray-300);
		background: var(--gradient-subtle);
		box-shadow: var(--shadow-sm);
	}

	@media (min-width: 50em) {
		.mention-card {
			border-radius: 1.5rem;
			height: 9.5rem;
		}
	}


	/* ========= レイアウト ========= */
	.profile-grid {
		/* 幅を制限して中央寄せ → 左端に張り付かない */
		max-width: 960px;
		margin-inline: auto;

		display: flex;
		flex-direction: column;
		gap: 2rem;
		align-items: center;
		text-align: center;
	}

	/* 大画面では左写真・右テキストの２カラム */
	@media (min-width: 50em) {
		.profile-grid {
		display: grid;
		grid-template-columns: 2fr 3fr;
		align-items: start;
		text-align: left;
		gap: 3rem;
		}
	}

	/* ========= 写真スタイル ========= */
	.profile-photo img {
		width: 100%;
		max-width: 320px;
		aspect-ratio: 1 / 1;
		object-fit: cover;
		border-radius: 1.5rem;
		box-shadow: var(--shadow-md);
	}

	/* テキスト側の幅と行間を整える */
	.profile-text {
		max-width: 48ch;
		line-height: 1.6;
	}

	/* ===== Works grid ===== */
	.works-grid {
		display: grid;
		gap: 3rem;
	}
	@media (min-width: 50em) {
		.works-grid {
		grid-template-columns: repeat(3, 1fr);
		gap: 4rem;
		}
	}

	/* ===== Work card ===== */
	.work-card {
		background: var(--gray-950);
		border: 1px solid var(--gray-800);
		border-radius: 1.25rem;
		padding: 2rem;
		box-shadow: var(--shadow-sm);
	}
	.work-card h3 {
		font-size: var(--text-xl);
		margin-bottom: 1rem;
	}
	.work-card .meta {
		display: flex;
		flex-direction: column;
		font-size: var(--text-sm);
		color: var(--gray-400);
		margin-bottom: 1rem;
		gap: 0.25rem;
	}
	@media (min-width: 40em) {
		.work-card .meta {
		flex-direction: row;
		justify-content: space-between;
		}
	}
	.work-card ul {
		margin-top: 1rem;
		list-style: disc;
		padding-left: 1.25rem;
		line-height: 1.6;
	}


</style>
