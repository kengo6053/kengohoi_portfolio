---
import { getCollection } from 'astro:content';

// Layout import — provides basic page elements: <head>, <nav>, <footer> etc.
import BaseLayout from '../layouts/BaseLayout.astro';

// Component Imports
import CallToAction from '../components/CallToAction.astro';
import Grid from '../components/Grid.astro';
import Hero from '../components/Hero.astro';
import Icon from '../components/Icon.astro';
import Pill from '../components/Pill.astro';
import PortfolioPreview from '../components/PortfolioPreview.astro';

// Page section components
import ContactCTA from '../components/ContactCTA.astro';
import Skills from '../components/Skills.astro';

// Content Fetching: List four most recent work projects
const projects = (await getCollection('work'))
	.sort((a, b) => b.data.publishDate.valueOf() - a.data.publishDate.valueOf())
	.slice(0, 4);

// Full Astro Component Syntax:
// https://docs.astro.build/basics/astro-components/

const skills = [
  {
    name: 'C',
    level: 5,
    desc: '実装で生産管理システムのバックエンド開発に使用。新人教育も担当しました。',
  },
  {
    name: 'HTML / CSS',
    level: 4,
    desc: 'LP・ブログのマークアップを多数経験し、アクセシビリティを重視。',
  },
  {
    name: 'Java',
    level: 3,
    desc: '大学講義とチーム開発で MVC アプリを実装。',
  },
  {
    name: 'Python',
    level: 4,
    desc: '研究で E2E 自動運転モデルを実装。PyTorch が主力。',
  },
  {
    name: 'TensorFlow / PyTorch',
    level: 4,
    desc: 'ディープラーニング案件で推論パイプラインを構築。',
  },
  {
    name: 'MATLAB',
    level: 3,
    desc: '研究初期の画像処理アルゴリズムのプロトタイピングに使用。',
  },
  {
    name: 'Linux',
    level: 4,
    desc: '日常的に Arch Linux。サーバ運用・シェルスクリプトが得意。',
  },
  {
    name: 'Arduino',
    level: 2,
    desc: '趣味で簡易 IoT ガジェットを製作。C++ によるファーム実装。',
  },
];
---

<BaseLayout>
	<div class="page-spacing">
		<div class="stack gap-10 lg:gap-24">
			<div class="wrapper stack gap-8 lg:gap-10">
				<header class="hero">
					<Hero
						title="Kengo Hoi"
						tagline="Welcome to my portfolio!"
						align="center"
					>
					</Hero>
				</header>
			</div>

			<!-- ========= ABOUT ========= -->
			<section id="about" class="section-space">
				<div class="wrapper stack gap-12 lg:gap-10">

					<!-- セクション見出し -->
					<header class="hero">
					<Hero
						title="About"
						tagline="私の簡単な自己紹介です。"
						align="center"
					/>
					</header>

					<!-- ========== 本体：左写真 / 右プロフィール ========== -->
					<div class="profile-grid">
					<!-- 左：写真 -->
					<div class="profile-photo">
						<img
						src="/images/my_photo
						.jpg"
						alt="Hoi Kengo portrait"
						width="320"
						height="320"
						loading="lazy"  
						/>
					</div>

					<!-- 右：プロフィールテキスト -->
					<div class="profile-text">
						<p><strong>名前：</strong>帆井 健悟 (Hoi&nbsp;Kengo)</p>
						
						<br>
						
						<p>
						<strong>学歴：</strong><br />
						・2020/03 渋谷教育学園幕張高等学校 卒業<br />
						・2025/03 北海道大学 工学部 情報エレクトロニクス学科 情報理工学コース 卒業<br />
						・2025/04 北海道大学大学院 情報科学院 情報理工学コース 調和系工学研究室 在籍 (修士1年)
						</p>

						<br>
						
						<p>
						<strong>興味のある技術分野：</strong><br />
						3DCG・XR・Interactive&nbsp;Graphics
						</p>
					</div>
					</div>
				</div>
			</section>

			<!-- ===== Skills ===== -->
			<section id="Skills" class="section-space">
				<div class="wrapper stack gap-14 lg:gap-12">
				<header class="hero-center">
					<Hero title="Skills" tagline="自分が持つスキルについての紹介です。" align="center" />
				</header>
				<div class="skills-grid">
					{skills.map(({name, level, desc}) => (
					<div class="skill-card">
						<h3>{name}</h3>
						<p class="stars">{'★'.repeat(level)}{'☆'.repeat(5-level)}</p>
						<p class="desc">{desc}</p>
					</div>
					))}
				</div>
				</div>
			</section>

			<!-- ========= Works ========= -->
			<section id="works"  class="section-space">
				<div class="wrapper stack gap-12 lg:gap-10">

					<!-- セクション見出し -->
					<header class="hero">
					<Hero
						title="Works"
						tagline="自分のこれまでの制作・活動実績を紹介します。"
						align="center"
					/>
					</header>

					<!-- ========= WORK LIST ========= -->
					<div class="works-grid">

					<!-- 1. ステレオカメラ E2E 自動運転研究 -->
					<article class="work-card" data-modal="modal-e2e">
						<h3>ステレオカメラを用いたE2E自動運転モデル</h3>
						<p class="meta">
						<span>北海道大学 / Astemo (共同研究)</span>
						<span>2023-10 – Present</span>
						</p>

						<p>
						従来の多センサ構成をステレオカメラ <em>単一</em> に集約し、
						TransFuser を改良して画像＋点群から加速度を予測する軽量
						E2E モデルを開発。<br />
						13 万フレームで学習した追従走行タスクで実用水準の性能を確認し、
						ステレオ専用アプローチの可能性を示唆。
						</p>

						<ul>
						<li>課題定義・検証設計・モデル設計をワンストップで推進</li>
						<li>大規模データ前処理を自動化するパイプラインを構築</li>
						<li>社内発表で<strong>高評価</strong>を獲得、継続開発中</li>
						</ul>
						…
					</article>

					
					<dialog id="modal-e2e" class="work-modal">
						<button class="close" aria-label="閉じる">×</button>
						<h3>ステレオカメラを用いた E2E 自動運転モデル</h3>
						<p class="meta"><span>北海道大学 / Astemo (共同研究)</span><span>2023-10 – Present</span></p>
						<p>ここに**より詳しい説明**を好きなだけ書けます。画像やリンク、コードなども可。</p>
					</dialog>

					<!-- 2. 広告薬機法チェック LLM 自動化 -->
					<article class="work-card">
						<h3>LLMを用いた薬機法チェック自動化システム</h3>
						<p class="meta">
						<span>受託開発 / 化粧品広告</span>
						<span>2024-04 – 2024-08</span>
						</p>

						<p>
						化粧品広告が薬機法に抵触しないかを
						<strong>大規模言語モデル</strong>で自動判定する Web サービスを
						2 名体制で開発。私は判定ロジックと API 連携を担当。
						</p>

						<ul>
						<li>プロトタイプ → フィードバック → 改善を隔週で反復</li>
						<li>弁護士事務所・仲介企業など多様な利害関係者と調整</li>
						<li>納期内リリース達成、運用コストを大幅削減</li>
						</ul>
					</article>

					<!-- 3. アボカド価格予測 ML コンペ -->
					<article class="work-card">
						<h3>アボカド価格予測機械学習コンペ</h3>
						<p class="meta">
						<span>大学授業プロジェクト</span>
						<span>2023-01 – 2023-02</span>
						</p>

						<p>
						3 名チームで米国アボカド価格を予測。
						私は<strong>ベースコード</strong>を整備し、特徴量設計・
						ハイパーパラメータ探索を効率化。未経験チームながら
						20 チーム中<strong>6位</strong>を獲得。
						</p>

						<ul>
						<li>LightGBM / XGBoost / CatBoost を並列で評価</li>
						<li>GitHub + Issues でタスク管理、全員の生産性を底上げ</li>
						<li>リーダーシップとドキュメンテーションの重要性を学習</li>
						</ul>
					</article>

					</div>
				</div>
			</section>

			<script is:inline>
			/* 開く */
			document.querySelectorAll('.work-card').forEach(card => {
			card.addEventListener('click', () => {
				const id = card.dataset.modal;
				const dlg = document.getElementById(id);
				if (dlg) dlg.showModal();
			});
			});

			/* 閉じる（× ボタン or 背景クリック） */
			document.querySelectorAll('.work-modal').forEach(dlg => {
			dlg.addEventListener('click', e => {
				if (e.target.classList.contains('close') || e.target === dlg) dlg.close();
			});
			});
			</script>

		</div>
	</div>
</BaseLayout>

<style>
	.hero {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 2rem;
	}

	.roles {
		display: none;
	}

	.hero img {
		aspect-ratio: 5 / 4;
		object-fit: cover;
		object-position: top;
		border-radius: 1.5rem;
		box-shadow: var(--shadow-md);
	}

	@media (min-width: 50em) {
		.hero {
			display: grid;
			grid-template-columns: 1fr;
			padding-inline: 2.5rem;
			gap: 3.75rem;
		}

		.roles {
			margin-top: 0.5rem;
			display: flex;
			gap: 0.5rem;
		}

		.hero img {
			aspect-ratio: 3 / 4;
			border-radius: 4.5rem;
			object-fit: cover;
		}
	}

	/* ====================================================== */

	.section {
		display: grid;
		gap: 2rem;
	}

	.with-background {
		position: relative;
	}

	.with-background::before {
		--hero-bg: var(--bg-image-subtle-2);

		content: '';
		position: absolute;
		pointer-events: none;
		left: 50%;
		width: 100vw;
		aspect-ratio: calc(2.25 / var(--bg-scale));
		top: 0;
		transform: translateY(-75%) translateX(-50%);
		background:
			url('/assets/backgrounds/noise.png') top center/220px repeat,
			var(--hero-bg) center center / var(--bg-gradient-size) no-repeat,
			var(--gray-999);
		background-blend-mode: overlay, normal, normal, normal;
		mix-blend-mode: var(--bg-blend-mode);
		z-index: -1;
	}

	.with-background.bg-variant::before {
		--hero-bg: var(--bg-image-subtle-1);
	}

	.section-header {
		justify-self: center;
		text-align: center;
		max-width: 50ch;
		font-size: var(--text-md);
		color: var(--gray-300);
	}

	.section-header h3 {
		font-size: var(--text-2xl);
	}

	@media (min-width: 50em) {
		.section {
			grid-template-columns: repeat(4, 1fr);
			grid-template-areas: 'header header header header' 'gallery gallery gallery gallery';
			gap: 5rem;
		}

		.section.with-cta {
			grid-template-areas: 'header header header cta' 'gallery gallery gallery gallery';
		}

		.section-header {
			grid-area: header;
			font-size: var(--text-lg);
		}

		.section-header h3 {
			font-size: var(--text-4xl);
		}

		.with-cta .section-header {
			justify-self: flex-start;
			text-align: left;
		}

		.gallery {
			grid-area: gallery;
		}

		.cta {
			grid-area: cta;
		}
	}

	/* ====================================================== */

	.mention-card {
		display: flex;
		height: 7rem;
		justify-content: center;
		align-items: center;
		text-align: center;
		border: 1px solid var(--gray-800);
		border-radius: 1.5rem;
		color: var(--gray-300);
		background: var(--gradient-subtle);
		box-shadow: var(--shadow-sm);
	}

	@media (min-width: 50em) {
		.mention-card {
			border-radius: 1.5rem;
			height: 9.5rem;
		}
	}


	/* ========= レイアウト ========= */
	.profile-grid {
		/* 幅を制限して中央寄せ → 左端に張り付かない */
		max-width: 960px;
		margin-inline: auto;

		display: flex;
		flex-direction: column;
		gap: 2rem;
		align-items: center;
		text-align: center;
	}

	/* 大画面では左写真・右テキストの２カラム */
	@media (min-width: 50em) {
		.profile-grid {
			display: grid;
			grid-template-columns: 2fr 3fr;
			align-items: center; 
			text-align: left;
			gap: 3rem;
		}
	}

	.page-spacing { padding-block: 4rem; }     /* モバイル基準 */
	@media (min-width: 50em) {
		.page-spacing { padding-block: 6rem; }   /* 大画面で拡大 */
	}
	
	/* ========= 写真スタイル ========= */
	.profile-photo img {
		width: 100%;
		max-width: 320px;
		aspect-ratio: 1 / 1;
		object-fit: cover;
		border-radius: 1.5rem;
		box-shadow: var(--shadow-md);
		align-items: center; 
	}

	/* テキスト側の幅と行間を整える */
	.profile-text {
		max-width: 48ch;
		line-height: 1.6;
	}

	/* ===== Works grid ===== */
	.works-grid {
		display: flex; 
		flex-direction: column;
		gap: 3rem;
	}
	@media (min-width: 50em) {
		.works-grid {
		grid-template-columns: repeat(3, 1fr);
		gap: 4rem;
		}
	}

	/* ===== Work card ===== */
	.work-card {
		background: var(--gray-950);
		border: 1px solid var(--gray-800);
		border-radius: 1.25rem;
		padding: 2rem;
		box-shadow: var(--shadow-sm);
	}
	.work-card h3 {
		font-size: var(--text-xl);
		margin-bottom: 1rem;
	}
	.work-card .meta {
		display: flex;
		flex-direction: column;
		font-size: var(--text-sm);
		color: var(--gray-400);
		margin-bottom: 1rem;
		gap: 0.25rem;
	}
	@media (min-width: 40em) {
		.work-card .meta {
		flex-direction: row;
		justify-content: space-between;
		}
	}
	.work-card ul {
		margin-top: 1rem;
		list-style: disc;
		padding-left: 1.25rem;
		line-height: 1.6;
	}
	.page-spacing {
  		padding-block: 4rem;   /* 上下 64px の余白 */
	}

	/***********************  Global helpers  ***********************/
	.section-space{padding-block:6rem;}
	.hero-center{display:flex;flex-direction:column;align-items:center;text-align:center;
	  margin-bottom: 3rem;   /* 例: 2.5rem ≒ 40px */}
	.wrapper{max-width:1200px;margin-inline:auto;padding-inline:1rem;}

	/***********************  Skills grid  *************************/
	.skills-grid{
		display:grid;
		grid-template-columns:repeat(auto-fill,minmax(260px,1fr));
		gap:1.5rem;
		margin-inline:auto;
		max-width:1200px;
	}
	.skill-card{
		background:var(--gray-950);
		border:1px solid var(--gray-800);
		border-radius:1rem;
		padding:1.5rem;
		box-shadow:var(--shadow-sm);
		transition:transform .2s,box-shadow .2s;
	}
	.skill-card:hover{
		transform:translateY(-4px);
		box-shadow:var(--shadow-md);
	}
	.skill-card h3{font-size:1.1rem;margin:0 0 .25rem 0;color:var(--gray-0);}
	.stars{color:var(--accent-regular);letter-spacing:.1em;margin-bottom:.5rem;}
	.desc{font-size:.875rem;line-height:1.5;color:var(--gray-300);}  

	@media(min-width:50em){
		.skills-grid{grid-template-columns:repeat(3,1fr);} /* 1行3つ固定 */
	}

	/* ===== Work modal (common) ===== */
	.work-modal{
	position: fixed;
	top: 50%; left: 50%;
	transform: translate(-50%, -50%);
	width: min(90vw, 760px);
	max-height: 80vh;
	overflow-y: auto;
	padding: 2.5rem 3rem;
	border: none;
	border-radius: 1rem;
	box-shadow: 0 14px 45px rgba(0,0,0,.55);
	z-index: 1000;
	}

	/* === 1) ライトモード用 === */
	@media (prefers-color-scheme: light) {
	.work-modal{
		background: #ffffff;          /* 真っ白いカード */
		color: #111111;               /* 濃い文字色 */
	}
	.work-modal::backdrop{
		background: #ffffff;    /* 背景を暗く */
	}
	}

	/* === 2) ダークモード用 === */
	@media (prefers-color-scheme: dark) {
	.work-modal{
		background: #1a1a1a;          /* 濃いグレーのカード */
		color: #f2f2f2;               /* 明るい文字色 */
	}
	.work-modal::backdrop{
		background: rgba(0,0,0,.75);  /* もう少し暗めでも可 */
	}
	}

	/* 閉じるボタン（共通） */
	.work-modal .close{
	position: absolute;
	top: .75rem;
	right: .75rem;
	background: none;
	border: none;
	font-size: 1.5rem;
	cursor: pointer;
	color: inherit;                 /* モードに合わせて自動で変わる */
	}

	/* ===== Safari など dialog 未対応ブラウザ用 ===== */
	@supports not (backdrop-filter:none) {
	.work-modal{
		inset: 0;
		margin: auto;
		transform: none;
		display: none;
	}
	.work-modal[open]{display:block;}
	.work-modal::backdrop{content:'';position:fixed;inset:0;}
	}

</style>
